{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web",
  "projectType": "application",
  "tags": ["type:app", "scope:web"],
  "// targets": "to see all targets run: nx show project web --web",
  "// build": "Build with webpack, not Turbopack: libs/contracts is declared type:commonjs (for the NestJS backend) but authored in ESM, which Turbopack rejects. Webpack transpiles it cleanly. See next.config.js.",
  "targets": {
    "build": {
      "options": {
        "command": "next build --webpack"
      }
    },
    "dev": {
      "options": {
        "command": "next dev --webpack"
      }
    }
  }
}
